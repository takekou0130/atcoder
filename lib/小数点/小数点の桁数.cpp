#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep2(i, s, n) for (int i = (s); i < (int)(n); i++)

typedef long long ll;

int n;

int main(){
  double n = 2.444;
  // setprecisionで小数点以下の桁数を指定できる
  cout << fixed << setprecision(10) << n << endl;
}
